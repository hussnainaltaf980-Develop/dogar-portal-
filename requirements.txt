fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
PyJWT==2.9.0   # security.py imports `jwt` directly (PyJWT) — was missing from deps
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.12
jinja2==3.1.4
aiofiles==24.1.0
reportlab==4.2.5
Pillow==10.4.0
pillow-heif==0.18.0   # decode iPhone .heic/.heif passport photos for OCR
rlPyCairo==0.4.0
pycairo==1.29.0
qrcode==7.4.2
freetype-py==2.5.1
pypdf==5.0.1
python-dateutil==2.9.0
email-validator==2.2.0
openai==2.38.0
PyYAML==6.0.3

# --- Optional features (Arabic text shaping for PDF + OCR fallback) ---
# These are gracefully imported with try/except in the codebase, so the
# app boots without them — but install them to enable the full feature
# set (Arabic letterheads + offline passport-image OCR via Tesseract).
arabic-reshaper==3.0.0
python-bidi==0.4.2
pytesseract==0.3.13   # also requires `tesseract-ocr` system binary

# --- Test dependencies (install with: pip install -r requirements.txt) ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
